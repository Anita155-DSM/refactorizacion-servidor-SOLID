//creamos una interface suguerida por el profe, con la intención de 
//con esto, le decimos qué va a devolver, o de qué tipo va a ser lo que devuelve,
//en este caso de tipo interface

export interface EmployeeInterface {
    name: string;
    position: string;
    baseSalary: number;
    yearsOfService: number;
    finalSalary: number;
}